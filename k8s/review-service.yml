apiVersion: apps/v1
kind: Deployment
metadata:
  name: cinerate-review-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cinerate-review-service
  template:
    metadata:
      labels:
        app: cinerate-review-service
    spec:
      containers:
      - name: review-service
        image: cinerate/review-service:latest
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: cinerate-review-service-svc
spec:
  type: ClusterIP
  selector:
    app: cinerate-review-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
