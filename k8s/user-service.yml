apiVersion: apps/v1
kind: Deployment
metadata:
  name: cinerate-user-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cinerate-user-service
  template:
    metadata:
      labels:
        app: cinerate-user-service
    spec:
      containers:
      - name: user-service
        image: cinerate/user-service:latest
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: cinerate-user-service-svc
spec:
  type: ClusterIP
  selector:
    app: cinerate-user-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
